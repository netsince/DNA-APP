## v0.2.1

v0.2.1 已完成制作，现已发布。

### 主要更新：

- 群聊：
    - 优化群聊提示词，修复切换指定角色发言时的人设错乱：
        - 历史发言自动标注说话者身份（"角色名：…"），模型可分辨每句话是谁说的；
        - 提示词注入全群成员身份清单与"当前发言角色"锁定指令，切换发言后仍严格贴合目标角色人设，不串戏。

- 世界：
    - 世界词条改为按对话动态激活（Lorebook）：
        - 聊天内容命中词条名称时，自动将该词条设定注入提示词，世界背景随剧情实时生效，未命中时不注入、节省上下文；
        - 激活词条额外透出人物属性与关系：性别、年龄、状态（如已故）、以及与该词条关联人物的关系；
        - 新增词条 sticky 机制：词条被激活后会在接下来若干轮内持续保留注入，避免世界设定反复横跳（轮数可配，0 表示禁用）。

- 对话：
    - 新增上下文与采样优化，缓解对话陷入循环、反复复读的问题：
        - 上下文保留条数可配置（0-1000）：单次请求最多携带的历史消息条数，防止长对话超出模型窗口导致退化；
        - system 新增"绝不复读"指令，要求不重复自己或对方上一条的话，每次给出新内容；
        - 采样参数可配置：温度、频率惩罚、存在惩罚（在「对话与策略」设置页调整），对易复读的模型可显著抑制复读；
        - 字数控制新增「适中」档（150-250 字）与「自定义」档：自定义档可填写最少/最多字数范围，介于严格与无限制之间，更贴合个人偏好。

    - 新增长对话记忆与上下文管理优化：
        - 在「上下文保留条数」上限之上，历史消息新增按 token 精确逐条裁剪（双重上限，两者叠加生效）：用当前模型 tokenizer 从最新往最老累计，超出预算即裁掉更早消息（token 预算可配，0-100000）；
        - 摘要改为累加式扩展：以旧摘要为基底追加新事实，不再每次重写，长对话不丢旧设定；
        - 摘要新增按词数触发（距上次摘要新增字符数超阈值），与轮数触发构成"双触发"（字符阈值可配，0 表示禁用）；
        - 新增作者注释（深度注入）：可设置一段始终希望模型强调的内容，按间隔周期性插入对话中间，而不只放在开头（间隔可配，0 表示禁用）；
        - 作者注释现已绑定到角色卡：可在角色卡编辑页设置，运行时角色卡优先、未设置时回退到全局设置，并随角色卡导出/导入透传。

- 身份与数据：
    - 身份新增「复制到剪贴板」导出与「从剪贴板导入」：在身份编辑页可导出/导入单个身份（JSON），方便分享与跨设备迁移，导入格式兼容直接的身份 JSON；
    - 全量数据 ZIP 备份现已包含身份：导出时打包身份，导入时（全部替换 / 仅追加两种模式）均支持身份恢复。

