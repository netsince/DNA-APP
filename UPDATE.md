## v0.2.2

v0.2.2 制作中。

### 主要更新：

- 修复：
    - 修复端侧语音合成（TTS）多段台词被吞问题：重构台词提取与括号过滤逻辑，针对大模型（LLM）在角色动作描写中漏打前引号、单边后引号等格式破损问题提供智能容错回退机制，确保只读台词的同时绝不丢弃后半段正文；
    - 修复省略号朗读异常（念出“坏坏”等拟音杂音）：在进入 Tokenizer 前将省略号（`...`、`……`、连续点等）统一映射为 ChatTTS 原生停顿标记 `[break_4]`，破折号（`——`、`～` 等）映射为短停顿标记 `[break_2]`，彻底根除自回归 Audio GPT 因未登录标点字符而产生的发音幻觉与异响；
    - 优化特殊符号清洗与标点去重：自动过滤 Markdown 标记字符（`*`、`#`、`_`、`~` 等）、Emoji 表情与未登录符号，并将连续重复标点（如 `！！！`、`？？？`）折叠为单字符，在保留情绪语调的同时防止自回归注意力错位；
    - 修复长文本朗读后半段变成电音嗡嗡声的问题：升级多级分句策略，针对缺乏句号但由分号/逗号连缀的超长单句进行安全长度限制与自适应子分句切分；同时在 Audio Code 自回归采样阶段增加连续重复 Code 监测与熔断截断机制，杜绝模型死循环退化导致 Vocos 声码器解码出数十秒高频电流蜂鸣/电音嗡嗡声。