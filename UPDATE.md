## v0.2.1

v0.2.1 制作中。

### 主要更新：

- AI 服务与采样调节：
    - 「AI 服务 → 采样参数」：温度、频率惩罚、存在惩罚、Top-P、Top-K、Min-P、重复惩罚、重复惩罚斜率（各参数均有默认值，可一键恢复）；
    - 对易复读的模型可在「AI 服务 → 采样参数」中调节频率惩罚 / 存在惩罚显著抑制复读；
    - DeepSeek 的 Base URL 固定为官方地址（与智谱一致），设置页与首次引导中隐藏 Base URL 输入框，用户仅需填写 API Key；
    - DeepSeek 服务商下，AI 服务页新增「思考模式」选项卡：可开关深度思考（thinking enabled/disabled），并选择思考强度（低 / 高 / 最高，对应 reasoning_effort），开启思考时采样参数（温度、top-p 等）将被 DeepSeek 忽略。

- 外观与体验：
    - 设置页改为分类入口：进入后先看到「主题与颜色 / 应用与启动 / 聊天界面」分组，再点击进入对应设置页，不再把所有开关和滑块平铺在一页；
    - 新增「上下文 Token 实时仪表盘」开关（默认关闭）：开启后在聊天输入栏上方实时显示上下文 Token 占用与预算，超预算时以警示色提示；
    - 新增「对话框透明度」调节：可调整聊天消息气泡的不透明度（0-100），数值越小气泡越透明、背景图透出越多，与「聊天背景遮罩强度」配合使用；
    - 新增「半屏聊天」开关：开启后聊天记录只显示在页面下半部分，上半部分留空方便查看背景；消息滚出上缘时气泡经渐隐淡出（列表保持透明、不做底色遮盖），过渡自然而非硬切；
    - 新增「消息气泡快捷按钮」四组开关：可分别控制 AI 消息气泡左上角头像、右上角「重说 / 复制 / 继续说」快捷按钮的显示，关闭后仍可通过长按 / 右键菜单使用；「重说 / 继续说」仅显示在最近一条 AI 消息上，头像在群聊中用于区分发言者。

- 对话与策略：
    - 设置页改为分类入口：进入后先看到「提示词策略 / 摘要与上下文 / 回复与发送 / 消息与高级」分组，再点击进入对应设置页，不再把所有设置项平铺在一页；
    - 新增「打开从此处分叉」开关（默认关闭）：开启后在聊天页右键对方气泡可「从此处分叉」，保留该气泡之前的内容另起新会话并自动打开；
    - 回溯到某条消息后，自动将被回溯丢弃的区间中最早的用户消息填入聊天输入框，方便从被打断的地方继续。
    - 新增「命令宏」与「正则替换」（默认启用，可在「对话与策略」关闭）：命令宏支持多种占位符（角色名、用户名、随机/选择、换行、掷骰等），在发送消息与快速回复时自动展开；正则替换可自定义「正则 → 替换为」规则，发送时按顺序应用，非法正则自动跳过。
    - 新增快速回复：输入栏上方显示一键发送按钮，支持分组归类，发送内容可使用上述宏占位符。
    - 新增上下文与复读优化：上下文保留条数可配置（0-1000），并加入「绝不复读」指令，每次给出新内容；字数控制新增「适中」档（150-250 字）与「自定义」档。
    - 新增长对话记忆与上下文管理：在保留条数之上新增按 Token 逐条裁剪（双重上限），摘要改为累加式扩展以免丢失旧设定，并新增按轮数或字符数触发的双触发；
    - 「作者注释」改为角色级专属（仅存在于角色卡高级选项，随角色卡导出/导入）：移除全局作者注释入口，作者注释仅在该角色卡的上下文中注入，缓存友好布局下以固定位置注入历史之后，不再按间隔插入历史中部。
    - 消息气泡新增轻微 Markdown 支持（加粗、斜体、行内代码、删除线）。

- 世界（世界书）：
    - 世界导入现支持 SillyTavern 世界书 JSON 格式，自动映射关键词、内容、排序、冷却/延迟等字段；
    - 世界词条改为按对话动态激活：聊天命中词条时自动注入设定，并透出人物属性与关系；新增 sticky 机制在若干轮内持续保留，避免反复横跳；
    - 词条新增高级匹配（附加关键词、正则触发、大小写/全词匹配、递归扫描、冷却/延迟、装饰符等），可精细控制命中；
    - 新增预算控制：可配置条数与 Token 上限，超出时自动裁剪并提示，避免撑爆上下文；
    - 缓存友好布局：激活词条改为独立 system 消息追加在历史之后，不再拼入系统提示词，保持「系统提示词 + 摘要 + 历史」前缀稳定，最大化 DeepSeek KV cache 命中率（命中缓存价格约为未命中的 1/10）。

- 会话列表：
    - 新增「置顶」：右键可置顶/取消，置顶会话排在前面并显示图钉标识（单聊与群聊均支持）；
    - 新增「复制会话」：右键可复制单聊/群聊，生成全新 ID 的完整副本（含消息与摘要，不复制置顶/归档状态），插入原会话之后；置顶会话也可直接复制。

- 身份与数据：
    - 身份新增「复制到剪贴板」导出与「从剪贴板导入」（兼容身份 JSON），方便分享与跨设备迁移；
    - 全量数据 ZIP 备份现已包含身份，导入支持全部替换 / 仅追加两种模式。

- 修复：
    - 修复导入备份 ZIP 时应用崩溃（OutOfMemoryError）。
    - 优化群聊提示词，修复切换指定角色发言时的人设错乱：历史发言自动标注说话者身份，并注入全群成员清单与「当前发言角色」锁定指令。
    - 修复替换角色图片后界面仍显示旧图、需重启才能生效的问题：根因是图片保存路径固定，Flutter ImageCache 以路径为 key 命中旧缓存。现改为每次保存生成带时间戳的唯一路径，替换后立即生效（IO 平台磁盘文件 / Web IndexedDB 均适用）；保存角色后自动清理被替换的旧图片文件，避免磁盘堆积。