## v0.2.0

v0.2.0 已完成制作，现已发布。

### 主要更新：

- 身份：
    - 你可以在侧边栏中的身份中创建身份
    - 可以填写名称、人设与介绍
    - 在创建会话时可以选择身份
    - 身份将会自动注入系统提示词与灵感的提示词中。

- 离线语音输入：
    - 在聊天页中，加入了“语音输入”功能。
    - 点击后，输入区域将会切换到语音输入页面。
    - 你可以点按进行录制或长按进行录制。
    - 需要预先在“设置” > “语音输入”中下载模型并开启“启用语音输入”。

- 离线语音合成：
    - 在聊天页中，加入了“语音合成”功能。
    - 在每个AI回复的左上角可以点击播放。
    - 可以为角色单独设置seed或全局设置seed。
    - 若本角色没有seed时，将会使用全局的seed。
    - 需要预先在“设置” > “语音合成”中下载模型并开启“启用语音合成”。

- 外观：
    - 主题：“设置” > “外观与体验”中加入了“主题”配置项。
        - 可在“主题”中切换“亮色”或“暗色”，亦或“跟随系统”。
    - 强调色：“设置” > “外观与体验”中加入了“强调色”配置项。
        - 可在“自定义”中随意更改强调色，亦或“自动”。
    - 底部导航栏：“设置” > “外观与体验”中加入了“主页底部导航栏”配置项。
        - 开启后，在主页、群聊、“我家”、“世界”底部显示导航栏，方便快速切换。默认关闭。
    - 自适应字体：少于6个字符但空间不足的文字，将会以缩小字号以保证不换行的策略进行显示。
    - 输入栏按钮布局优化：
        - 在聊天页中，若输入框没有消息，将会显示“自动括号”、“灵感”与“语音”按钮。
        - 若有消息，则显示“自动括号”与“发送”按钮。
    - 聊天背景遮罩强度调节：“设置” > “外观与体验”中加入了“聊天背景遮罩强度”配置项。
        - 聊天界面背景图上的遮罩层不透明度数值越大，背景越暗，文字越清晰。
        - 0 为完全不遮罩，100 为遮罩最强。
        - 默认为75。
    - 横屏下，将会强制显示侧边栏。

- 交互优化：
    - 底部提示显示时长：
        - “设置” > “外观与体验”中加入了“底部提示显示时长”配置项。
        - 控制复制、保存等底部提示停留的时间。
        - 可选择1到10秒的显示时长。
    - 快捷括号：
        - 在“聊天页”中，加入了“快捷括号”功能，位于输入框的右边。
        - 点击后，将会自动在聊天框末尾插入“（）”并将光标移动至“（）”内。
        - 可在“设置” > “对话与策略”中，关闭“输入框旁显示括号按钮”。
    - 开屏引导（OOBE）：
        - 现在“开屏引导”可以被跳过。
        - 在“开屏引导”的“配置API”步骤，可以选择“服务商”。

- 世界：
    - 世界支持导入与导出，与角色卡导入导出相同。

- 删除：
    - 会话、群聊会话、TA、世界：
        - 支持归档后删除，默认需要输入被删除条目的名称才可删除。
        - 若在“设置” > “安全与隐私”中关闭了“删除前强制输入名称”后，可以直接长按10s后删除条目。
    - 单条消息删除：
        - 当在“设置” > “对话与策略”中，开启了“任意删除对话项”后，你可以长按一个消息后选择“删除本条”。

- 自动备份：
    - 开启后，每天首次进入应用时，自动在软件外部（公共目录）生成一份全量备份，保留最近 5 天。
    - 本功能默认开启
    - 可在“设置” > “数据管理”中配置。

- 图标：
    - 重构了图标选择界面。
    - 新增了6个图标。
    - Windows下支持以快捷方式选择图标的方式切换图标。

- 关于界面：
    - 在设置中加入了关于界面。
    - 将会显示参与人员、相关链接与开源许可证。

### 其他更新：

- “服务商”加入了“DeepSeek”。
- 导出角色卡时，将会导出seed。

### Bug修复：

- 强调色为自动时，聊天界面的强调色生成失败。导致聊天界面仍然使用系统的强调色。此问题已在本版本修复。


**Full Changelog**: https://github.com/netsince/DNA-APP/compare/v0.1.9...v0.2.0