## v0.2.2

v0.2.2 制作中。

### 主要更新：

- 设置与体验全面重构（降低认知门槛）：
    - 设置主页全面结构化升级：重塑为四大模块化卡片功能区（核心 AI、视觉与安全、语音与多模态、系统与数据），配备圆角徽标与功能指引副标题，视觉呼吸感与条理性大幅提升；
    - AI 服务设置体验优化：增加「未选定模型」醒目提示条，避免用户切换服务商后遗漏选定模型导致无法聊天；拉取到模型列表后自动选中首项；优化模型列表为可滚动独立容器，杜绝海量模型平铺拉伸页面；
    - 聊天界面设置文案精简：大刀阔斧精简背景遮罩、对话框透明度等长篇啰嗦的技术说辞，全面换用圆角卡片分组，视觉更加清爽；
    - 端侧语音合成与缓存深度整合：重构 TTS 控制台，嵌入本地音频缓存占用实时计算与一键清空能力，升级台词朗读与全局音色 Seed 场景化调节引导；
    - 快速回复管理体验升级：新增动态宏占位变量语法（{{char}} / {{user}} / {{random}} / {{newline}}）顶部引导卡片，优化回复卡片分组展示与快捷编辑；
    - 数据管理设置结构强化：明确区分「全量数据备份与还原 (ZIP)」与「单项对话导出与导入 (JSON)」，精炼每日自动备份说明，降低操作顾虑；
    - 安全与隐私设置精简：重写实体删除防误触确认项，砍掉冗长细则，直接圈出「输入名称确认」与「长按 5 秒确认」的核心区别；
    - 应用与启动设置直观化：将技术化的 Token 仪表盘直观命名为「聊天记忆容量仪表盘 (Token)」，清晰解释记忆容量与上下文空间概念；
    - 主题与颜色设置升级：换用分段选择按钮，清晰呈现「自动取色（提取角色立绘主色增强沉浸）」与「自定义单色」的体验差异；
    - 消息正则清洗页指引补全：顶部新增场景引导卡片，列举过滤 AI 口癖、消除动作描写标记与修正常见错别字的实际用途；
    - 语音输入节点说明补全：单选列表明确标注 ModelScope 国内高速直连与 GitHub 官方源的区别；
    - 回复与发送设置重构：采用卡片分组，清晰说明回车行为、动作括号快捷键、灵感生成与重试队列策略；
    - 摘要与上下文设置重构：将世界书 `sticky` 等硬核技术黑话重构为通俗的「词条附着持续轮数」，并清晰阐明对话轮数与字数双重触发机制及 Token 预算裁剪规则；
    - 消息与高级进阶设置补全：全面补全单条消息删除、剧情节点分叉、动态占位宏变量（{{char}} / {{user}} 等）与消息正则文本清洗的实际应用场景说明。

- TA 与人设编辑：
    - TA 形象槽改为一行并排展示（1:1 头像、16:9 横版名片、9:16 全屏立绘），大幅压缩纵向高度并清晰标注使用场景；
    - 「设定」与「介绍」概念明确与输入形态强区分：保留原名并增添辅助说明（设定标注发送给 AI 的角色人设，介绍标注仅用于卡片展示的一句话简介）；「介绍」限制为单行输入框，「设定」调整为默认多行大输入框；
    - 「对话风格」语气范例优化：示例对话列表明确标注「你的发言（输入）」与「TA 的回答（输出）」，并增加功能说明，消除对发言身份归属的困惑；
    - 「我的身份」编辑页精简：移除冗余的介绍输入框，并将人设调整为默认多行输入框。

- 对话策略与采样调节：
    - 提示词策略页排版优化：优化卡片间距与字号层级，消除密集感与压迫感，并为推进策略与沉浸策略的每个选项补充详细的场景化效果说明；
    - 采样参数页新增场景预设：提供「标准平衡」、「天马行空」、「长篇叙事」、「强力防复读」四种典型场景一键套用，滑块数值联动更新，同时保留专业级参数手动微调。

- 世界（世界书）：
    - 创建与编辑世界体验重构（渐进式分层设计）：
        - 核心必填信息突出：主面板仅保留世界名称与世界背景两个核心项，配合场景化占位文案，大幅减轻初次创建世界的认知负担；
        - 高级字段默认折叠：简介备注、分类标签、禁止输出词语等选填项统一收纳至折叠面板，按需展开；
        - 子词条管理体验升级：移除平铺式的庞大输入表单，改为卡片列表展示；新增与编辑词条采用底部抽屉弹窗，并区分基础触发设定与高级规则（正则/全词/递归/冷却延迟/装饰符等），同时支持点击已有词条直接修改。

- 修复：
    - 修复端侧语音合成（TTS）多段台词被吞问题：重构台词提取与括号过滤逻辑，针对大模型（LLM）在角色动作描写中漏打前引号、单边后引号等格式破损问题提供智能容错回退机制，确保只读台词的同时绝不丢弃后半段正文；
    - 修复省略号朗读异常（念出“坏坏”等拟音杂音）：在进入 Tokenizer 前将省略号（`...`、`……`、连续点等）统一映射为 ChatTTS 原生停顿标记 `[break_4]`，破折号（`——`、`～` 等）映射为短停顿标记 `[break_2]`，彻底根除自回归 Audio GPT 因未登录标点字符而产生的发音幻觉与异响；
    - 优化特殊符号清洗与标点去重：自动过滤 Markdown 标记字符（`*`、`#`、`_`、`~` 等）、Emoji 表情与未登录符号，并将连续重复标点（如 `！！！`、`？？？`）折叠为单字符，在保留情绪语调的同时防止自回归注意力错位；
    - 修复长文本朗读后半段变成电音嗡嗡声的问题：升级多级分句策略，针对缺乏句号但由分号/逗号连缀的超长单句进行安全长度限制与自适应子分句切分；同时在 Audio Code 自回归采样阶段增加连续重复 Code 监测与熔断截断机制，杜绝模型死循环退化导致 Vocos 声码器解码出数十秒高频电流蜂鸣/电音嗡嗡声。