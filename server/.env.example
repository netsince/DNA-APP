# 服务监听
HOST=0.0.0.0
PORT=5011
FLASK_DEBUG=False

# 必须自行生成并设置，切勿使用默认值
# 生成方式：python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

APPNAME=与汝共奏 广场

# MySQL（你正在操作的即生产库，任何改表前请先全量备份）
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=
