# 服务监听
HOST=0.0.0.0
PORT=5011
FLASK_DEBUG=False

# 必须自行生成并设置，切勿使用默认值
# 生成方式：python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

APPNAME=dna-server

# MySQL（你正在操作的即生产库，任何改表前请先全量备份）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=dna
DB_PASSWORD=
DB_NAME=dna
